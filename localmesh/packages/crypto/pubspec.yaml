name: crypto_layer
description: Cryptographic operations for LocalMesh — pure Dart, no Flutter dependencies.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  domain:
    path: ../domain
  cryptography: ^2.7.0
